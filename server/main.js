import {Meteor} from 'meteor/meteor';

import "../imports/api/AgencyList";
import "../imports/api/RouteList";
import "../imports/api/Routeconfig";

Meteor.startup(() => {
    // code to run on server at startup

});
